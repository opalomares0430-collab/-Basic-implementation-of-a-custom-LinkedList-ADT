# -Basic-implementation-of-a-custom-LinkedList-ADT
Implement a custom LinkedList ADT with an iterator in Java. The LinkedList should support the basic operations:  insertion deletion traversal using an iterator
